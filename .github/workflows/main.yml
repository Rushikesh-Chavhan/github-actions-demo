# .github/workflows/email-step.yml
name: Email Action Test

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: 24f3002795@ds.study.iitm.ac.in
        run: echo "Hello, world!"
